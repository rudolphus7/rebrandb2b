name: Hourly Sync

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Sync API
        run: |
          curl -X GET "${{ secrets.SITE_URL }}/api/sync?key=${{ secrets.CRON_SECRET }}"
